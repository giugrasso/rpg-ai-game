# Redirige les chemins inconnus vers index.html pour SPA
error_page 404 /index.html;

location = /index.html {
    internal;
}

# Pour API non trouvée, renvoyer un JSON clair
location /api/ {
    default_type application/json;
    return 404 '{"error":"API endpoint not found"}';
}
